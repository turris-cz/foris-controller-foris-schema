{
    "oneOf": [
        {
            "properties": {
                "module": {"type": "string"},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        }
    ]
}
